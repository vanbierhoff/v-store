
export interface StoreFieldOptionsInterface  {
    /**
     * In strict mode, a check will be performed before setting the value.
     * In this mode, it is possible to set only a valid value in the store
     */
    strictSet: boolean;
}
